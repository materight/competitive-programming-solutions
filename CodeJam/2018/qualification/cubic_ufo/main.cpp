#include <fstream>
#include <iostream>

using namespace std;

typedef long double ld;

int main(int argc, char** argv) {
    int T;
    cin >> T;

    ld A;
    for (int t = 0; t < T; t++) {
        cin >> A;
    }

    return 0;
}